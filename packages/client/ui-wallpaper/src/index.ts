/**
 * Web wallpaper plugin, node half.
 *
 * Deliberately empty: the browser half applies the wallpaper stylesheet, and
 * the host Loader has no behavior to contribute.
 */

/** Host plugin body — presentation lives in the client half. */
export function apply(): void {}
